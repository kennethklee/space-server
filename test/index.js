var app = require('../index'),
	ioc = require('socket.io-client');

describe('Server', function() {
	it('Login should spawn a player', function(done) {
		//var client = ioc('ws://0.0.0.0:54017');

		//client.on('connect', function() {
			done();
		//});

	});
});